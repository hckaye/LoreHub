# ADR 0003: lh command-line interface

[English](0003-lh-command-line-interface.md) | [日本語](0003-lh-command-line-interface.ja.md)

- Status: Proposed
- Date: 2026-08-13

## Context

LoreHub exposes its features over the `/api/v1` HTTP API, authenticated by browser sessions or personal access
tokens (`lhp_` prefixed, sent as `Authorization: Bearer`). The only official client is the web UI. Repository
data-plane operations (clone, sync, push) belong to the separate `lore` CLI, which signs in with
`lore auth login --token-type api-key` using a LoreHub personal access token.

GitHub users drive the same class of workflows with the `gh` CLI. Teams adopting LoreHub ask for the equivalent:
scripting issues and pull requests, inspecting CI runs, creating releases, and raw API access from the terminal.

## Decision

Ship an official CLI named `lh` that mirrors the `gh` command taxonomy against the LoreHub API.

### Placement and toolchain

- The CLI lives in this repository under `cli/`, as its own Go module (`cli/go.mod`), producing one binary named
  `lh`. A separate module keeps server-internal packages and their dependencies out of the CLI and lets the CLI
  be released standalone.
- Commands are built on `spf13/cobra`, matching the structure `gh` uses: `lh <resource> <verb>` with global
  flags, per-command flags, and `lh help` output generated by the framework.
- The CLI speaks only the public `/api/v1` HTTP API. It never imports server internals, so it exercises the same
  surface as any third-party client.

### Authentication and configuration

- `lh auth login` stores a personal access token per host. Interactive mode prompts for the token; `--with-token`
  reads it from stdin for scripts. `lh auth logout` removes the stored token; `lh auth status` reports the host,
  the authenticated user (via the API), and the token's permissions.
- Hosts and tokens live in `~/.config/lh/hosts.yml` (respecting `XDG_CONFIG_HOME`), one entry per host, mode
  0600. Environment overrides follow `gh` conventions: `LH_TOKEN` wins over the hosts file, `LH_HOST` selects
  the host when a command is not run inside a repository context.
- Personal access tokens cannot manage other tokens, so `lh auth login` never creates tokens itself; it explains
  where to create one in the web UI. A future device-authorization flow can replace token pasting without
  changing the command surface.

### Command taxonomy (first iteration)

| Command | Verbs | Notes |
| --- | --- | --- |
| `lh auth` | `login`, `logout`, `status` | token-based, per host |
| `lh repo` | `list`, `view`, `create`, `clone` | `clone` resolves the repository's `lores://` URL, runs `lore auth login --token-type api-key` with the stored token, then `lore` clone; fails with guidance when the `lore` CLI is absent |
| `lh issue` | `list`, `view`, `create`, `comment`, `close`, `reopen` | filters mirror the web list (state, author, assignee, label, milestone, search) |
| `lh pr` | `list`, `view`, `create`, `merge` | `merge` drives the server-side Lore merge flow and reports blockers; local checkout is out of scope because Lore has no detached PR refs |
| `lh release` | `list`, `view`, `create` | |
| `lh run` | `list`, `view`, `watch` | Actions workflow runs; `watch` polls until completion |
| `lh label` | `list`, `create`, `delete` | |
| `lh search` | `repos`, `issues`, `prs` | maps to the search API |
| `lh api` | raw requests | `lh api repos/{owner}/{repo} --method POST --field k=v`, prints raw JSON; escape hatch for everything not yet wrapped |

- Repository context: inside a directory whose repository is known (flag `--repo owner/name` always wins), the
  CLI resolves `owner/name` the way `gh` does. Because Lore working copies are managed by the `lore` CLI, the
  first iteration requires `--repo` or an interactive default set via `lh repo set-default`; deriving context
  from a Lore working copy is a follow-up once the `lore` CLI exposes it.
- Output: human-readable tables on a TTY, tab-separated plain output when piped, and `--json` on list/view
  commands emitting the API response for scripting. Errors print the API problem message and exit non-zero.
- Localization: command output starts English-only, like `gh`. The web dictionaries are not reused.

### Testing and release

- Unit tests cover command parsing, config/host resolution, and API interactions against `httptest` servers.
- `npm run cli:*` scripts mirror the existing `api:*` scripts (`gofmt` check, `go vet`, `go test`, `go build`)
  so CI covers the CLI with the same gates as the server.
- Distribution starts with `go build` artifacts; packaging (Homebrew, installers) is out of scope here.

## Rejected alternatives

### Extend the lore CLI

The `lore` CLI is the VCS data-plane client owned by the Lore project. Folding forge workflows (issues, pull
requests, CI) into it couples two release cycles and two ownership domains. `gh` and `git` stay separate for the
same reason.

### Node.js CLI sharing the web front end code

A Node CLI could reuse TypeScript API types, but it drags a runtime dependency onto build agents and servers
where Go ships a single static binary. The Go API client is small and generated-by-hand against a stable API.

### Same Go module as the server

Importing `services/api` internals would give the CLI private types for free, but it welds the CLI to server
releases, bloats its dependency graph, and hides accidental use of non-public behavior. The CLI must live on the
public API only.

## Operational consequences

- The public API becomes a compatibility surface for scripts; breaking changes to `/api/v1` now break `lh` users
  and must be called out in release notes.
- `lh repo clone` depends on an installed `lore` CLI; the command degrades with an actionable message when it is
  missing.
- New documentation: `docs/lh-cli.md` (+ Japanese) covering install, login, and the command table above.
